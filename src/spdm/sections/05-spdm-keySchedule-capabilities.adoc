
[[SPDM_registration]]
=== SPDM Key Schedule Registration Properties

Each SPDM Key Schedule algorithm capability advertised is a self-contained JSON object using the following values.

[[SPDM_keySchedule_caps_table]]
.SPDM Key Schedule Algorithm Capabilities JSON Values
|===
| JSON Value | Description | JSON Type | Valid Values

| algorithm | The algorithm to be validated | string | "KDF"
| mode | The Ascon mode to be validated | string | "SPDM"
| revision | The algorithm testing revision to use | string | "1.0"
| keyLen | The supported ley lengths in bits | Domain | {"Min": 128, "Max": 4096, "Inc": 8}
| thLen | The supported th string lengths in bits | Domain | {"Min": 256, "Max": 512, "Inc": 128}
| spdmVersion | The version(s) of SPDM supported | array of strings | "spdm1.1", "spdm1.2", "spdm1.3"
| usesPSK | The support for a pre-shared key | array of booleans | true, false
| hashAlgs | The hashing algorithms supported | array of strings | "SHA2-256", "SHA2-384", "SHA2-512", "SHA3-256", "SHA3-384", "SHA3-512"
|===

[[SPDM_keySchedule_capabilities]]
==== SPDM Key Schedule Mode Capabilities Example

Below is an example of the registration for KDF / SPDM / 1.0

[source, json]
----
{
  "algorithm": "KDF",
  "mode": "SPDM",
  "revision": "1.0",
  "isSample": false,
  "keyLen": [
    {
      "min": 128,
      "max": 4096,
      "increment": 8
    }
  ],
  "thLen": [
    {
      "min": 256,
      "max": 512,
      "increment": 128
    }
  ],
  "spdmVersion": [
    "spdm1.1",
    "spdm1.2",
    "spdm1.3"
  ],
  "usesPSK": [
    true,
    false
  ],
  "hashAlgs": [
    "SHA2-256",
    "SHA2-384",
    "SHA2-512",
    "SHA3-256",
    "SHA3-384",
    "SHA3-512"
  ]
}

----
